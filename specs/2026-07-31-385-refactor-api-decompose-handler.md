# Spec: Decompose `internal/api/handler.go` into route-group files (#385)

- **Issue:** MumuTW/looper#385 — `refactor(api): decompose handler.go into route-group handlers`
- **Base branch:** `main`
- **Type:** Maintainability refactor of `internal/api` (no behavior change)
- **Source:** Code Tomography Scan RC-001 / UM-001

## Problem

`internal/api/handler.go` is **7,988 lines** with **246 top-level functions** (83 of them `*Handler` methods) covering eight unrelated concerns in one file: health, status/version, config, loops (start/stop/retry/discard/logs), webhooks (forward + stats), projects, pull requests, runs/active-runs, workers/planners, and HITL/Feishu. The `Context` struct carries 15+ function fields and the `Handler` struct is the single sink for every route.

The cost is concrete, not aesthetic:

- **Navigation.** A change to webhook forwarding forces opening a 7,988-line file and scrolling past health/status/config/loops to reach `buildWebhookForwardResponse` (line 585) and `buildWebhookStatusResponse` (line 1369). Reviewers cannot scope a diff to one concern.
- **Coupled review surface.** Any route-group edit lands in the same file as the admission gate, the `serveHTTP` router, and the lock helpers, so a trivial config-response tweak re-opens the most security-sensitive code in the package for review.
- **Test co-location pressure.** `handler_test.go` is already 8,832 lines and `loop_retry_discard_test.go` is 2,051 lines; the single-file production code pushes tests toward the same monolith because there is no narrower unit to attach them to.

The repo already started decomposing this file by concern — `loop_retry_discard.go` (617 lines), `loop_logs_stream.go` (473 lines), `issue_occupancy.go`, `pull_request_lookup.go`, `config_patch.go`, `request_decode.go`, `bootstrap_routes.go`, `browser_guard.go` — all in **`package api`**, same package, no new types. That pattern works; `handler.go` is simply the part that has not yet been split.

## Goals

- Reduce `handler.go` to the router plus the cross-cutting machinery it must own: the `Context`/`Handler` types, `NewHandler`, `ServeHTTP`/`serveHTTP`, authorization/admission, lock helpers, request-ID, and the shared `apiError`/`writeJSON` plumbing.
- Move each route group's response builders and route handlers into a dedicated file named after the concern, so a reviewer can scope a diff to one concern.
- Keep the decomposition **mechanical and behavior-preserving**: no route moves, no JSON shape changes, no status-code changes, no lock-ordering changes, no test-expectation changes.
- Preserve the existing same-package file-split convention already established by `loop_retry_discard.go` et al.
- Land in reviewable slices, each independently green (`go build ./...`, `go vet ./...`, `go test ./...`).

## Non-goals

- **No new `internal/api/handlers/` subpackage and no per-concern handler structs.** See *Approach* for why the subpackage + struct-per-concern design from the issue is rejected in favor of same-package file splits.
- No change to the `Context` struct field set, the `Handler` method set, or the public `NewHandler` signature. `cmd/looperd/main.go:156` is the only production caller and must not move.
- No re-routing, no middleware reordering, no admission-gate logic changes.
- No test rewrite. Tests construct `api.Context{...}` directly (163 sites in `handler_test.go`, 9 in `handler_hitl_test.go`); the refactor must not force those call sites to change.
- No extraction of the lock helpers (`lockLoopRetry`/`lockLoopTarget`/`lockLoopTargetForStatus`) or the admission machinery out of `handler.go` — they are shared by multiple route groups and moving them is a separate decision.

## Authority

> **What is the authority for this action, and why is it not the agent's own structured output?**

Not applicable — this is a pure refactor with no new gate, validation, persisted field, or "verify before acting" check. No new authority is introduced; the existing admission authority (ADR-0015 / #575) and lock authorities (`looperdruntime.LockLoopRequeue`, `LockLoopTarget`) stay exactly where they are. Recorded only because the design guidelines require the question to be answered when touching agent-driven action paths, and the honest answer is "nothing new is enforced."

## Approach

Same-package file decomposition, matching the convention already established by `loop_retry_discard.go`, `loop_logs_stream.go`, `issue_occupancy.go`, `pull_request_lookup.go`, `config_patch.go`, `request_decode.go`, `bootstrap_routes.go`, and `browser_guard.go`. Each new file stays in `package api`, keeps the `*Handler` receiver, and moves only the functions and types that belong to one route group.

### Why not the issue's `internal/api/handlers/` subpackage + per-concern structs

The issue proposes a new `internal/api/handlers/` package where each handler is its own struct holding only its specific dependencies, and the main `Handler` becomes a router. That is a new layer and a new concept (per-concern handler structs with their own dependency surfaces), so per AGENTS.md it must answer the two trade-off questions:

> **Delete this six months from now — what breaks?**

Nothing the file split alone does not already prevent. The pain being fixed is "one 7,988-line file." A subpackage does not make the file shorter than same-package splits do; it adds an import boundary and a per-concern struct on top of the split that already solves the navigation problem.

> **What does it still not catch?**

It does not catch cross-concern coupling through the `Context` struct (15+ function fields shared by every route group), because each new struct would still receive a slice of the same `Context`. It also does not catch the real coupling — the shared lock helpers and admission gate — which the subpackage would either duplicate or re-import, trading one import cycle risk for another.

The simpler move — same-package file splits — solves the stated problem (file size, review scoping, test co-location) without:

- **A new cross-package API surface.** A subpackage forces every moved function to either export or be wrapped, and forces `Handler` to hold/construct one struct per concern. Same-package splits keep the unexported `buildHealthResponse`/`buildStatusResponse`/etc. unexported and called directly from `serveHTTP`.
- **Test churn.** Tests build `api.Context{...}` directly in 163+ sites. A subpackage would either change the construction path or require a parallel test context type. Same-package splits leave every test construction untouched.
- **A new abstraction to keep in sync.** Per-concern handler structs are a concept that must be taught, reviewed for correct dependency injection, and kept aligned as `Context` evolves. The existing `Handler` already *is* the router via the `serveHTTP` switch; splitting files does not require re-deriving that.

Recorded per "Prefer deletion over another layer": the layer-adding direction (subpackage + structs) was considered and rejected because the layer-removing direction (split the file, keep the package) achieves the maintainability goal with strictly less surface. The conclusion is "no subpackage."

### File plan

All new files live in `internal/api/`, `package api`. Each moves only the functions/types listed; `handler.go` keeps the router, types, and shared plumbing. Order is chosen so each slice compiles and tests green independently — health/status/config/webhook first (leaf response builders with few cross-references), loops last (most entangled with locks, retry/discard, logs).

1. **`internal/api/health.go`** — `healthResponse`, `storageHealth`, `migrationHealth`, `buildHealthResponse`. (~30 lines moved)
2. **`internal/api/status.go`** — `statusResponse`, `statusService`, `statusTriage`, `statusBinary`, `versionResponse`, `versionBinaryResponse`, `statusStorage`, `statusScheduler`, `statusAgent`, `statusAgentTimeouts`, `statusAgentRoleTimeouts`, `statusWebhook`, `statusLoopType`, `statusLoops`, `statusSafety`, `statusNotifications`, `statusTools`, `buildStatusResponse`, `buildVersionResponse`, `daemonBinaryStatus`, `daemonExecutablePath`, `buildWorktreeCleanupStatusResponse`, `buildNetworkStatusResponse`, `storageState`, `loadStorageState`, `startedAtISO`, `schemaVersion`, `countLoops`, `sumStatusCounts`, `summarizeWebhookStatus`, `looperdArtifactName`, `normalizeRecoverySummary`, `recoveryWithOutstanding`, `statusDegradedReasons`, plus the status-only leaf helpers `hasValue`, `homeDirOrEmpty`, and `currentLooperdTarget`. (~620 lines moved)

   `summarizeWebhookStatus` is the daemon-wide status projection consumed by `buildStatusResponse`; it stays here. `hasValue`, `homeDirOrEmpty`, and `currentLooperdTarget` move with the status slice: a repo-wide production caller audit shows all three are used only by `buildStatusResponse` (`handler.go:1448-1533`), so leaving them in `handler.go` would contradict the single-caller rule and strand status implementation (and its `os`/`filepath` imports) in the router file. `looperdArtifactName` is a status-only helper whose sole production caller is `buildStatusResponse` (`handler.go:1450`), so it moves with the status slice rather than staying behind in `handler.go`. The raw `buildWebhookStatusResponse` builder that the `/webhook/status` router branch calls directly moves with the other webhook route code into `webhook.go` (see item 4), so the webhook route group is not split across two files. `serverBaseURL` is **not** moved here: its sole production caller is `buildWebhookStatusResponse` (`handler.go:1380`), so it moves to `webhook.go` with that builder (see item 4) to keep the whole `/webhook/status` implementation in one file.
3. **`internal/api/config.go`** — `ConfigFieldMetadata`, `ConfigMetadata`, `configResponse`, `configRolesResponse`, `configServerResponse`, `configAgentResponse`, `configDaemonResponse`, `configPackageResponse`, `handleConfigRoute`, `buildConfigResponse`, `buildConfigMetadata`, `sortedMapKeys`, `cloneAgentProfiles`. (~250 lines moved)
4. **`internal/api/webhook.go`** — `buildWebhookForwardResponse`, `buildWebhookStatusResponse`, `serverBaseURL`, `isLoopbackRequest`. (~90 lines moved)

   `buildWebhookStatusResponse` is the raw builder the `/webhook/status` router branch calls directly (`handler.go:365`), so it moves with the forwarding endpoint to keep the whole webhook route group in one file. `serverBaseURL` moves here too: a repo-wide search shows its sole production caller is `buildWebhookStatusResponse` (`handler.go:1380`), so sending it to `status.go` would split the `/webhook/status` implementation across two files; co-locating it with its only caller keeps the whole raw route group together. `isLoopbackRequest` is webhook-only (its sole caller is `buildWebhookForwardResponse`). `hasForwardingProxyHeaders` and `isLoopbackRemoteAddr` are **not** moved: they are shared authorization helpers used by `authorizeRequest` and `isDirectLoopbackConfigMutation` to enforce wildcard-auth and config-mutation restrictions, so they stay beside the authorization code in `handler.go` (see the "After all slices land" list).
5. **`internal/api/loops.go`** — the loop route builders and handlers that are not already in `loop_retry_discard.go` / `loop_logs_stream.go`: `loopsListResponse`, `maxLoopsListLimit`, `loopResponse`, `retryLoopRequest`, `retryLoopResponse`, `buildLoopsRouteResponse`, `parseLoopsListOptions`, `buildLoopRouteResponse`, `retryLoop`, and the loop start/stop/retry/discard route entry points that remain in `handler.go` today. The loop-log response types `loopLogsResponse`, `loopLogsRunResponse`, and `loopLogsAgentPayload` are **not** listed here — they move to `loop_logs_stream.go` with the rest of the log slice (see the relocation paragraph below), because `buildLogsStateForRun` constructs all three and `loopLogsResponse` is consumed throughout `loop_logs_stream.go` (the combined streaming path and the chunk/state helpers all take it); keeping them in `loops.go` would split the log response contract from its builders and streaming logic. `retryLoop` currently lives in `handler.go` (not `loop_retry_discard.go`, which holds only the worktree/discard helpers), so it moves here with the rest of the loop route/response layer; the discard path already extracted into `loop_retry_discard.go` stays put.

   The slice also owns the loop-creation and lifecycle blocks that the route builders call and that otherwise remain stranded in `handler.go` (same-package compilation stays green while they stay, so the per-slice review must move them explicitly rather than rely on `go build`):

   - **Loop creation** (`handler.go:4097-4343`): the `createLoopRequest` type, `(h *Handler) buildCreateLoopResponse`, and the loop-only target projection helpers `loopTargetIDCompat`, `repoFromTargetCompat`, and `prNumberFromTargetCompat` (`handler.go:7292-7311`). A repo-wide production search shows those three are called only by the candidate/record construction inside `buildCreateLoopResponse` (`handler.go:4259-4285`), so they move with the loop-creation slice rather than staying in shared plumbing. The intermixed worker/planner request and response types (`createWorkerRequest`, `createPlannerRequest`, `workerCreateResponse`, `plannerCreateResponse`) move to `workers.go` (see item 10); only `createLoopRequest`, `buildCreateLoopResponse`, and the three loop-only `*Compat` projections belong to `loops.go`.
   - **Loop lifecycle handlers** (`handler.go:5572-5995`): `(h *Handler) resolveLoop`, `(h *Handler) mutateLoopStatus`, `takeoverLoopResponse`, `(h *Handler) takeoverLoop`, `(h *Handler) handbackLoop`, `retryRequestRequestsDiscard`, `respondLoopRequest`, `(h *Handler) respondToLoop`, and `(h *Handler) deliverHumanAnswer`. The coding-role resume/retry gates `(h *Handler) assertCodingRoleResumeAgent` (`handler.go:7586`, called by the `/start` lifecycle path at `handler.go:5670`) and `(h *Handler) assertCodingRoleRetryAgent` (`handler.go:7578`, a thin wrapper that delegates to `assertCodingRoleResumeAgent` and is called by the retry path at `handler.go:7030`) move here too: both are loop-lifecycle-only, so leaving them in `handler.go` strands route-specific gating in the router file. The shared predicate `isCodingRoleAgentConfigured` they call is used by loop, worker, and planner creation as well, so it stays in `handler.go` as shared plumbing (named in the "After all slices land" inventory).
   - **Loop serialization and creation helpers** (`handler.go:6623-6950`): `(h *Handler) serializeLoopWithDiagnostics`, `buildLoopTarget`, `decorateLoopDiagnostics`, and the loop metadata/reviewer helpers `normalizeMetadataJSON`, `manualFixerMetadataJSON`, `reviewerLoopMetadataJSON`, `isValidReviewerCleanReviewEvent`, `removeDeprecatedReviewerLoopBudgetMetadata`, the package-level `deprecatedReviewerLoopBudgetMetadataKeys` (`handler.go:6885-6891`, consumed only by `removeDeprecatedReviewerLoopBudgetMetadata`), `isDeprecatedReviewerLoopBudgetReason`, `isValidReviewerBlockingReviewEvent`, `isTerminalReviewerLoopRecord`, and `rejectDiscardWhileParkedForHuman`. `decorateLoopDiagnostics` (`handler.go:3452`) moves here, not to `runs.go`: its only production call sites are `buildLoopsRouteResponse` (`handler.go:2266`) and `serializeLoopWithDiagnostics` (`handler.go:6663`), both of which move to `loops.go`, so it is an exclusively loop-response helper; leaving it in the runs route file would recreate the cross-concern review surface the decomposition removes. `serializeLoop` (`handler.go:6623`) and `manualPlannerMetadataJSON` (`handler.go:6742`) are **not** loop-only — `serializeLoop` is also called by `buildWorkersCreateResponse`/`buildPlannersCreateResponse` and `manualPlannerMetadataJSON` by the planner create path — so they stay in `handler.go` as shared plumbing (named in the "After all slices land" inventory) per the shared-helper rule in *Risks*.
   - **Retry-precondition and discard-precondition helpers** (`handler.go:6951-7417`): `(h *Handler) assertDiscardSharedPRWorktreeClear`, `isDiscardBlockingSiblingPRStatus`, `assertNoActiveSiblingPRWorktreeLoops`, `(h *Handler) assertLoopRetryPreconditions`, `terminalReviewerRetryMetadataStatus`, `resetReviewerLoopRetryMetadata`, `resetFixerLoopRetryMetadata`, and `mapLoopCreateError`. The genuinely cross-route `*Compat` target/queue helpers in this same range (`buildQueuedLoopQueueRecordCompat`, `buildWorkerQueuePayloadJSONCompat`, `loopTargetKeyCompat`, `assertUniqueActiveLoopCompat`, `loopTargetKeyFromRecordCompat`, `loopTargetFromRecordCompat`, `normalizeProjectTargetID`, `parseIssueNumber`) are **not** loop-only — they are called by `buildWorkersCreateResponse`/`buildPlannersCreateResponse` and the retry path as well — so they stay in `handler.go` as shared plumbing (named in the "After all slices land" inventory) per the shared-helper rule in *Risks*. The three `*Compat` target projections `loopTargetIDCompat`, `repoFromTargetCompat`, and `prNumberFromTargetCompat` are **not** in this shared group: their only production callers are in `buildCreateLoopResponse`, so they move to `loops.go` with the loop-creation block above.

   Moving the four loop-only blocks above (creation, lifecycle, serialization, retry/discard preconditions) is what keeps `handler.go` at the retained-plumbing floor (~1,400 lines; see the final-inventory note); omitting any of them leaves well over a thousand loop-specific lines behind while every prescribed build check still passes, so the declaration-inventory check — not `go build` — is what catches the omission. (~1,800 lines moved, including the route/response layer and the four loop-only blocks)

   The loop-log implementation that is still in `handler.go` moves into the existing **`internal/api/loop_logs_stream.go`** (not `loops.go`), because that file already owns the combined streaming path and already calls `buildLogsStateForRun`. Specifically, the loops slice also relocates: the five `loopLogsFollow*` constants (`loopLogsFollowPollInterval`, `loopLogsFollowStatePollInterval`, `loopLogsFollowRetryAfter`, `loopLogsFollowMaxChunkBytes`, `loopLogsFollowSnapshotBytes`, `handler.go:49-53`) — these are consumed only by the loop-log route/stream implementation (the moved `streamLoopLogs` and the existing `loop_logs_stream.go`), so leaving them in the shared `const` block would strand loop-log policy and its `time` dependency in the router file after the stream code moves; the loop-log SSE event types `loopLogsFollowChunkEvent` and `loopLogsFollowErrorEvent` (`handler.go:678-694`) — the moved `streamLoopLogs` and `newLoopLogsFollowErrorEvent` instantiate them and the existing `loop_logs_stream.go:305` already uses the chunk type, so leaving either behind would split the stream-only declarations from the streaming code that owns them; the follow/streaming route functions `isFollowLoopLogsRequest`, `streamLoopLogsRoute`, `streamLoopLogs`, `newLoopLogsFollowErrorEvent`, `queryBool`, `writeSSEEvent`, `loopLogsStreamState`, `shouldDefaultLoopLogsStreamToStderr`, `appendedLogChunk`, `shouldTerminateLoopLogsFollow`, `shouldTerminateLoopLogsFollowBeforeChunk` (`handler.go:3887-4095`); the loop-log response types `loopLogsResponse`, `loopLogsRunResponse`, and `loopLogsAgentPayload` (`handler.go:1877-1907`) — `buildLogsStateForRun` constructs all three and `loopLogsResponse` is consumed throughout `loop_logs_stream.go` (the combined streaming path at `loop_logs_stream.go:23`/`192`/`207`/`272`/`302` and the moved `loopLogsStreamState`/`shouldDefaultLoopLogsStreamToStderr`/`shouldTerminateLoopLogsFollow*` helpers all take it), so co-locating them with the log slice keeps the response contract beside its builders and streaming logic rather than splitting it into `loops.go`; the run-log endpoint in `runs.go` still references `loopLogsResponse` via the cross-file same-package call into `buildLogsResponseForRun`, which needs no import; the log snapshot builders `buildLoopLogsResponse`, `buildLogsResponseForRun`, `buildLogsStateForRun` (`handler.go:6530-6621`, the latter already called by `loop_logs_stream.go`); and the persisted-agent-log helpers `maxPersistedAgentLogReadBytes`, `agentOutputPayload`, `decodeAgentOutput`, `materializeAgentOutput`, `readAgentOutputLog`, `isPathWithinDirectory` (`handler.go:7434-7508`, the last shared with `loop_logs_stream.go`). The generic JSON helpers `parseJSONObject`, `readStringAny`, `firstNonEmptyString`, `stringPtrOrNil` (`handler.go:7510-7568`) are **not** moved — they are used by runs/workers/pull-requests as well, so they stay in `handler.go` per the shared-helper rule in *Risks*. `readObject`, `readObjectString`, and `readStringMap` are **not** in that shared group: a production caller audit shows all three are used only by the active-run worktree projection (`handler.go:3805-3814`), so they move to `runs.go` with that block (see item 8) rather than staying behind as shared plumbing. `buildRunLogsResponse` is run-route-only and moves with `runs.go` (see item 8), calling `buildLogsResponseForRun` across files in the same package.
6. **`internal/api/projects.go`** — `projectsListResponse`, `projectResponse`, `projectValidationResponse`, `discoveryResponse`, `createProjectResponse`, `projectService`, `buildProjectsRouteResponse`, `buildProjectRouteResponse`, `buildProjectDiscoverResponse`, `projectDiscoverResponse`, plus the project mutation and serialization helpers that those route builders call and that otherwise remain stranded in `handler.go`: `createProjectRequest`, `updateProjectStringField` (with its `UnmarshalJSON` method), `updateProjectRequest`, `updateProjectField`, `buildCreateProjectResponse` (called by `buildProjectsRouteResponse`), `buildUpdateProjectResponse` (called by `buildProjectRouteResponse`), `serializeDiscovery`, `serializeProject` (called by the create/update/discover paths), `serializeProjectValidation`, `resolveProjectProviderKind` (sole caller is `serializeProject`), `stringMetadataValue` (sole caller is `resolveProjectProviderKind`), `deriveProjectIDFromRepoPath` (the `handler.go` copy at ~line 7963, sole caller is `buildCreateProjectResponse`) and its project-only regexp `nonProjectIDPattern` (`handler.go:58`, sole caller is `deriveProjectIDFromRepoPath`), and `decodeProjectIdentifier` (the project path decoder whose sole production caller is `buildProjectRouteResponse` at `handler.go:2137`; it wraps the shared `decodePathSegment`, which stays in `handler.go` — see the "After all slices land" inventory). Moving `nonProjectIDPattern` with `deriveProjectIDFromRepoPath` keeps the project-id normalization (and its `regexp` dependency) out of the router file once the helper moves. The shared project-metadata helpers `parseProjectMetadata` and `stringMetadataPtr` (the `map[string]any` overload at ~line 7928) are used by loops/workers too, so they stay in `handler.go` per the shared-helper rule in *Risks*; `normalizeOptionalString` is likewise shared and stays. (~440 lines moved)
7. **`internal/api/pull_requests.go`** — `pullRequestsListResponse`, `pullRequestResponse`, `pullRequestStatusResponse`, `pullRequestLoopStatus`, `pullRequestActionability`, `pullRequestIdentity`, `buildPullRequestsRouteResponse`, `buildPullRequestRouteResponse`, `buildPullRequestStatusResponse`, `findPullRequestLoops`, `serializePullRequestListItem`, `derivePullRequestActionability`, `pullRequestSnapshotDetail`, `checksBlockMerge`, `checksPending`, `reviewBlocksMerge`, `reviewPending`, `boolPtrIfPresent`, `stringFromMap`, `boolPtr`, `snapshotString`, `pullRequestKey`, `groupPullRequestLoops`, `dedupeLatestSnapshots`, `collectPullRequestIdentities`, `findLatestLoopStatus`, `pullRequestLoopStates`. (~450 lines moved)
8. **`internal/api/runs.go`** — `runsListResponse`, `runResponse`, `activeRunsListResponse`, `activeRunsQuery`, `activeRunView`, `activeRunTarget`, `activeRunAgent`, `activeRunWorktree`, `buildRunsRouteResponse`, `buildRunRouteResponse`, `buildActiveRunsResponse`, `buildActiveRunRouteResponse`, and the full active-run helper block that those builders call: `buildActiveRunDetailResponse`, `buildActiveRunViews`, `excludeActiveRunViewsByLoopID`, `listDefaultActiveRunLoops`, `latestRunsForDefaultActiveRuns`, `manualResumeCandidateLoopStatuses`, `latestRunsByLoopID`, `loopIDsFromLoops`, `latestQueueItemByLoopID`, `isManualInterventionQueue`, `hasManualInterventionResumePolicy`, `isClosedLoopStatus`, `decorateActiveRunView`, `shouldUseRunSummaryAsFailureReason`, `isBackingOffQueue`, `resumePolicyFromRun`, `buildVerifiedActiveAgentByRunID`, `buildActiveAgentByRunID`, `groupActiveExecutionsByRunID`, `buildActiveRunAgents`, `preferredActiveRunAgent`, `isPlausiblyLiveActiveRun`, `runHeartbeatIsRecent`, `runningLoopWithoutRunIsFresh`, `loadProjectNamesByIDForActiveRunTargets`, `hasProjectActiveRunTarget`, `projectNamesByID`, `tryBuildActiveRunTarget`, `readActiveRunsQuery`, `matchesActiveRunQuery`, `compareActiveRunViews`, `buildWorktreeSummary`, plus the run serialization and run-log helpers that the route builders call and that otherwise remain stranded in `handler.go`: `serializeRun` (sole production caller is `buildRunsRouteResponse` at `handler.go:2340`) and `buildRunLogsResponse` (sole production caller is `buildRunRouteResponse` at `handler.go:2990`). `buildRunLogsResponse` calls the shared `buildLogsResponseForRun`/`buildLogsStateForRun`, which move to `loop_logs_stream.go` (see item 5); the cross-file call stays in `package api` and needs no import. This is the contiguous `handler.go` block from `buildActiveRunsResponse` (~line 2882) through `buildWorktreeSummary` (~line 3803), together with `serializeRun` (~line 6667) and `buildRunLogsResponse` (~line 6546); leaving any of it behind would strand hundreds of run-specific lines in `handler.go` and miss the ~1,400-line target (the floor the retained shared-plumbing inventory establishes; see the final-inventory note). The run-only declarations that the final shared inventory had wrongly retained also move here: the `activeRunExecutionVerifier` interface (`handler.go:66`, used only by the active-run verifier cast at `handler.go:3525`), the `activeRunHeartbeatTTL` constant (`handler.go:54`, used only by active-run freshness checks at `handler.go:3132` and `handler.go:3603`), and the parsing/projection helpers `mapKeys`, `parsePositiveInt64`, `readObject`, `readObjectString`, and `readStringMap` — a production caller audit shows all five are used only inside the active-run block, so they move with `runs.go` rather than staying in `handler.go` as shared plumbing. `decorateLoopDiagnostics` is **not** in this slice: its only callers are loop-response builders, so it moves to `loops.go` (see item 5). (~1,360 lines moved)
9. **`internal/api/events.go`** — `eventsListResponse`, `entityEventsResponse`, `eventResponse`, `buildEventsRouteResponse`, `buildEntityEventsRouteResponse`, `serializeEvent`, and `parsePayloadJSON` (the event payload decoder whose sole production caller is `serializeEvent` at `handler.go:2839`; a repo-wide search finds no other production caller, so leaving it behind would strand event response logic in the shared-only `handler.go`). (~130 lines moved)
10. **`internal/api/workers.go`** — the worker/planner create route builders and the worker/planner-specific helpers they call that otherwise remain stranded in `handler.go`. Same-package compilation stays green while these stay, so the per-slice review must move them explicitly rather than rely on `go build`:

   - **Request/response types** (`handler.go:4110-4141`): `createWorkerRequest`, `createPlannerRequest`, `workerCreateResponse`, `plannerCreateResponse`.
   - **Create route builders**: `(h *Handler) buildWorkersCreateResponse` (`handler.go:4377`) and `(h *Handler) buildPlannersCreateResponse` (`handler.go:5182`).
   - **Worker reuse and manual-hold helpers** (`handler.go:4904-5181`): `derefBool` is shared with loop creation (see the "After all slices land" inventory) and stays in `handler.go`; the worker/planner-only helpers move: `reusableWorkerLoopForIssueRequestCompat`, `(h *Handler) resumeReusableWorkerLoopCompat`, `forcedManualWorkerQueuePayloadJSONCompat`, `forceManualWorkerLoopStateCompat`, `forcedManualWorkerMetadataJSONCompat`, `issueWorkerMetadataJSON` (also called by `buildCreateLoopResponse`, so it stays in `handler.go` as shared plumbing, named in the final inventory), `forcedManualWorkerCheckpointJSONCompat`, `stringPtrEqual`, `reusedWorkerResponseFields`. `(h *Handler) validateManualHoldBypassForLoopTarget` and `(h *Handler) refreshTargetLabels` are called by both loop and worker creation, so they stay in `handler.go` as shared plumbing (named in the final inventory); `(h *Handler) refuseOccupiedIssueTarget` is worker/planner-only and moves.
   - **Worker/planner project and PR-target resolution** (`handler.go:5298-5465`): `resolveWorkerProjectInput`, `(h *Handler) resolveWorkerProject`, `requirePullRequestTargetInput`, `(h *Handler) requirePullRequestTarget`, `(h *Handler) resolveUnsnapshottedPullRequestTarget`, `(h *Handler) acceptFreshPullRequestTarget`, `closedPullRequestTargetError`.
   - **Planner lookup and title helpers** (`handler.go:5470-5560`): `findPlannerLoopForIssueInput`, `workerPlannerMatch`, `(h *Handler) maybeFindPlannerLoopForIssue`, `(h *Handler) getPlannerPullRequestOpenState`, `deriveWorkerTitle`, `normalizePositiveInt64Ptr`, `int64MetadataPtr`.

   The issue-claim admission helpers (`assertIssueClaimAdmission`, `upsertLoopAfterIssueClaimAdmission`, `issueCollisionError`, `mapIssueClaimAdmissionError`, `handler.go:4343-4377`) are called by both loop and worker/planner creation, so they stay in `handler.go` as shared plumbing (named in the final inventory) per the shared-helper rule in *Risks*; only the worker/planner-only declarations above move to `workers.go`. (~600 lines moved)
11. **`internal/api/hitl.go`** — `handleFeishuCardActionRoute` and the complete HITL card-action helper set: the `feishuCardActionEnvelope` type (`handler.go:5996-6043`, the request envelope decoded by `handleFeishuCardActionRoute` and passed to the reply handler), `(h *Handler) handleFeishuThreadReply` (`handler.go:6153-6198`, the free-text reply consumer invoked from `handleFeishuCardActionRoute` at `handler.go:6112`), and the remaining HITL card-action helpers. Both declarations stay callable across the same package, so the prescribed build checks pass while they remain in `handler.go`; naming them explicitly is what keeps the "moves only listed declarations" rule from silently leaving roughly one hundred HITL-only lines behind. (~remaining HITL lines moved)

After all slices land, `handler.go` should contain only: package/imports, the `const`/`var` block of shared constants and regexps (excluding the route-specific `loopLogsFollow*` constants, which move to `loop_logs_stream.go`, and `nonProjectIDPattern`, which moves to `projects.go`), `RuntimeState`, `Context`, `PullRequestTarget`, `TakeoverResult`, `Handler`, `effectiveConfig`, `NewHandler`, the lock helpers, `ServeHTTP`/`serveHTTP` (the router), `apiError` (including its `(apiError).Error` method, a separate top-level declaration at `handler.go:696`)/`asAPIError`/`internalServerError`/`assertMethod`/`isMutatingHTTPMethod`/`isAdmissionExemptMutationPath`/`admissionMutationDenial`/`admissionStateString`, `authorizeRequest`/`isDirectLoopbackConfigMutation`/`isLoopbackRemoteAddr`/`hasForwardingProxyHeaders` (the shared authorization helpers, kept beside the auth code that calls them), `normalizePath`, `writeSuccess`/`writeError`/`writeJSON`, `generateRequestID`, and the small shared helpers (`parseJSONObject`, `readStringAny`, `firstNonEmptyString`, `stringPtrOrNil`, `parseProjectMetadata`, `stringMetadataPtr`, `normalizeOptionalString`, `derefString`, `isCodingRoleAgentConfigured`, `validateCodingProjectRunnable`), plus the genuinely cross-route-group helpers that no single listed destination can own cleanly:

- **Path decoders** — `decodePathSegment` (used by events via `buildEntityEventsRouteResponse` and by projects via `decodeProjectIdentifier`) and `urlPathSegment` (used by loops and runs). Both stay in `handler.go`; `decodeProjectIdentifier` itself is project-only and moves to `projects.go` (see item 6).
- **Project/loop validation** — `requireActiveProjectRecord` (used by loop creation/retry/discard and by worker/planner creation), `validateLoopTargetProjectCompatibility` (used by loop creation and worker/planner creation), `isCodingRoleAgentConfigured` (used by loop, worker, and planner creation), and `validateCodingProjectRunnable` (used by loop and worker creation). The loop-lifecycle-only retry/resume gates `assertCodingRoleRetryAgent` and `assertCodingRoleResumeAgent` are **not** shared — they move to `loops.go` (see item 5).
- **Loop/worker shared serialization and metadata** — `serializeLoop` (called by loops and by `buildWorkersCreateResponse`/`buildPlannersCreateResponse`), `manualPlannerMetadataJSON` (called by loop creation and planner creation), `issueWorkerMetadataJSON` (called by loop creation and worker creation), `derefBool` (called by loop and worker creation), `validateManualHoldBypassForLoopTarget` and `refreshTargetLabels` (called by loop and worker creation).
- **Issue-claim admission** — `assertIssueClaimAdmission`, `upsertLoopAfterIssueClaimAdmission`, `issueCollisionError`, `mapIssueClaimAdmissionError` (called by loop, worker, planner creation and the retry/takeover path).
- **Target/queue `*Compat` helpers** — `buildQueuedLoopQueueRecordCompat`, `buildWorkerQueuePayloadJSONCompat`, `loopTargetKeyCompat`, `assertUniqueActiveLoopCompat`, `loopTargetKeyFromRecordCompat`, `loopTargetFromRecordCompat`, `normalizeProjectTargetID`, `parseIssueNumber` (called by loop, worker, planner creation and the retry/discard path). The three target projections `loopTargetIDCompat`, `repoFromTargetCompat`, and `prNumberFromTargetCompat` are **not** in this shared group — their only production callers are in `buildCreateLoopResponse`, so they move to `loops.go` (see item 5).

Every helper in those five groups is used by ≥2 route groups, so it stays as shared plumbing per the rule in *Risks*; naming them here is what makes the final-contents check enforceable — a "moves only listed declarations" slice that leaves any of them behind is now an explicit deviation rather than a silent omission. Target: under ~1,400 lines.

This is a floor, not an aspiration. The declarations explicitly retained in the inventory above occupy **~1,255 source lines** in the parent by themselves (measured by parsing `internal/api/handler.go`: `RuntimeState`/`Context`/`PullRequestTarget`/`TakeoverResult`/`Handler`/`apiError`, `NewHandler`/`effectiveConfig`, the three lock helpers, `ServeHTTP`/`serveHTTP`, the admission/auth/write plumbing, `generateRequestID`, the path decoders, the project/loop validation helpers, the loop/worker shared serialization/metadata helpers, the issue-claim admission helpers, and the target/queue `*Compat` helpers). Even a perfect implementation of every listed move therefore leaves ~1,255 declaration lines plus the package/import block, the shared `const`/`var` block, declaration separators, and comments — i.e. ~1,380–1,400 lines total. A gate below that floor (the earlier ~1,200) is unattainable by construction and would fail the whole-refactor validation regardless of slice completeness; the revised gate sits just above the realistic post-move size, so that any route-group code left behind (hundreds of lines) pushes the file over it while a complete decomposition passes. Moving more shared plumbing out to hit a lower number is rejected because it reverses the documented shared-helper rule (helpers used by ≥2 route groups stay in `handler.go`); the honest fix is to set the gate at the floor the inventory implies.

### Slice ordering and verification

Each slice is one commit, ordered so no slice leaves a dangling reference:

1. health → 2. status → 3. config → 4. webhook → 5. events → 6. projects → 7. pull_requests → 8. runs → 9. workers → 10. loops → 11. hitl.

Loops and hitl are last because they have the most cross-references into locks, retry/discard, and the Feishu handshake; moving them last minimizes cross-reference churn and keeps each intermediate slice's diff focused on leaf response builders first. Note: moving declarations between files that all remain in `package api` cannot change the package import graph and therefore cannot create a Go import cycle, regardless of slice ordering — same-package compilation is not a guard on ordering here, and (as the rest of this spec observes) it also does not detect stranded declarations. The ordering is a reviewability/churn choice, not a compile-time constraint. After each commit run:

```
gofmt -l .
go vet ./...
go run honnef.co/go/tools/cmd/staticcheck@v0.6.1 -tests=false -checks='U1000,SA1006,SA4004,SA4006' ./...
go test ./...
go build ./...
# declaration-inventory check: parse handler.go + the slice's destination file and
# diff their top-level declaration names against the File plan lists. go build stays
# green while declarations are stranded in package api, so it cannot be this guard.
```

## Risks

- **Splitting a function that is shared across route groups.** Several helpers are used by more than one group — e.g. `serializeLoop` (loops and worker/planner create), `decodePathSegment` (events and projects), `urlPathSegment` (loops and runs), and `derefBool` (loop and worker creation). Mitigation: a helper used by ≥2 groups stays in `handler.go` (shared plumbing) or moves to the file of its *primary* user with the other caller importing nothing new (same package). Decide per-helper during the slice; record the decision in the commit body. A helper with a single production caller is **not** "shared" — it moves with that caller (e.g. `serverBaseURL` → `webhook.go`, `looperdArtifactName` → `status.go`, `parsePayloadJSON` → `events.go`, `sortedMapKeys` → `config.go`, `boolPtr`/`stringFromMap`/`snapshotString` → `pull_requests.go`); the same-package build stays green either way, so the per-slice review must check caller counts, not just compilation.
- **Accidentally moving a `*Handler` method that the router still calls, or stranding an associated declaration.** Because everything stays in `package api` with the same receiver, moving a method is purely a file relocation — the call site in `serveHTTP` is unchanged, and `go build` does **not** detect a wrong or incomplete move: a method that references a type, request/response struct, const, or helper left behind in `handler.go` still compiles, because same-package access needs no import. Forgetting to move an associated declaration is therefore *not* a build error — it is the stranded-declaration failure this spec calls out in every slice that names explicitly-moved blocks (items 5, 10, 11 and the loop-log relocation all note that "same-package compilation stays green while they stay"), and a slice can be declared green by `go build` while violating the decomposition. Mitigation: each slice must pass an **explicit declaration-inventory check** — parse `handler.go` and the destination file and diff their top-level declaration names against the lists in *File plan* — so that a declaration left behind, moved to the wrong file, or omitted is an explicit deviation rather than a silent one. `go build`/`go vet`/staticcheck remain required per slice but are guards on imports and unused code, not on decomposition correctness; the inventory check is the authority for "moves only listed declarations," not the build.
- **Import drift / unused imports.** Each new file needs its own `import` block. Mitigation: `goimports`/`gofmt` per slice; `go vet` flags unused.
- **Test-file growth smell.** This refactor adds **no new production state** — it only relocates code, and adds tests only to close the one documented `/webhook/status` coverage gap (see *Validation*). Per the test-growth guideline, the right framing is: the relocation is neither propping up nor covering; the single new route-level test is *covering* logic that shipped untested, which is the tell that cutting it would make the codebase worse. If a reviewer asks, the answer is "no tests were added for behavior that was already covered; the one new test closes a pre-existing hole so the suite can guard the endpoint being moved."
- **Hidden behavior coupling in `serveHTTP`.** The router reads `h.context.Config` and re-snapshots it for `/config` in `ServeHTTP` before delegating. That logic stays in `handler.go` untouched; no slice touches it.
- **Second-fix signal.** This is the first decomposition PR for `handler.go`; there is no prior `fix:` to the same area to trigger the revert rule. If a follow-up `fix:` lands shortly after, treat it as a signal that a slice moved something it should not have, and prefer reverting the offending slice over stacking a patch.

## Validation

- **Per slice:** `gofmt -l .` clean, `go vet ./...` clean, staticcheck (production checks) clean, `go test ./...` green, `go build ./...` green, **and a declaration-inventory check** confirming `handler.go` and the slice's destination file contain exactly the declarations *File plan* assigns them (same-package compilation stays green while declarations are stranded, so `go build` cannot be the authority for decomposition correctness — see *Risks*).
- **Whole refactor:** `handler.go` under ~1,400 lines (floor-justified — see the *File plan* final-inventory note: the retained declarations alone occupy ~1,255 source lines, so the gate sits at the realistic post-move floor, not below it); the declaration set of `handler.go` matches the retained inventory in *File plan* and contains nothing else — verified by an **explicit declaration-inventory check** (e.g. parse `handler.go` and diff its top-level declaration names against the retained list), **not** by `go build`, because same-package compilation stays green while stranded declarations remain and therefore cannot be the authority for decomposition correctness (see *Risks*); no new files outside `internal/api/`; `cmd/looperd/main.go` unchanged; `git diff --stat` shows net line movement (production lines roughly conserved, no meaningful net growth).
- **Behavior parity (the regression net):** the existing test suite is the contract. Specifically:
  - `handler_test.go` (8,832 lines) and `handler_hitl_test.go` exercise the full route table and must pass unchanged.
  - `loop_retry_discard_test.go`, `loop_logs_stream_test.go`, `loop_start_stop_gate_test.go`, `loop_retry_stop_gate_test.go`, `worker_reuse_stop_gate_test.go` cover the lock/retry/discard invariants and must pass unchanged.
  - `config_routes_test.go`, `config_projection_test.go`, `config_patch_test.go` cover config response shape and must pass unchanged.
  - `non_mutating_http_test.go`, `wildcard_auth_test.go`, `config_auth_test.go` cover admission/auth and must pass unchanged.
  - `root_handler_test.go`, `server_test.go` cover routing and must pass unchanged.
  - **Coverage gap to close in the webhook slice:** `/api/v1/webhook/status` has no route-level test today (a repo-wide search finds the path only in `handler.go` and user docs). Before slice 4 relocates `buildWebhookStatusResponse`, that slice must add a route-level regression test asserting the method, routing, and raw JSON response shape of `/api/v1/webhook/status`, so the suite can detect a method/routing/response regression on this documented endpoint once the builder moves. This is *covering* logic that shipped untested, not propping up a state machine — the narrow, justified exception to the "no new tests" stance below.
- **No new test files except the documented coverage gap.** A slice adds a test only to close the `/webhook/status` gap above; otherwise, if a slice cannot compile without a new test, the slice is wrong, not the tests.
- **CI `verify` job** (`.github/workflows/ci.yml`) is the final gate: dashboard → gofmt → go vet → staticcheck → go test → go build.

## Out of scope (explicit)

- Splitting `handler_test.go` / `handler_hitl_test.go` into per-concern test files. Tempting and consistent, but it is a separate, test-only refactor with its own churn risk; doing it in the same PR would hide production moves behind test moves. Track as a follow-up.
- Reducing the `Context` struct field count or introducing a dependency-injection container. That is a real coupling reduction but a behavior-adjacent change; this PR is strictly mechanical.
- Moving the lock helpers or admission gate out of `handler.go`. They are shared; moving them is a separate decision with its own trade-off write-up.
