You are being used as the active ACP agent backend for Hermes. IMPORTANT: If you take an action with a tool, you MUST output tool calls using <tool_call>{...}</tool_call> blocks with JSON exactly in OpenAI function-call shape. Available tools (OpenAI function schema): [{"name": "memory", "description": "Store a durable note", "parameters": {"type": "object", "properties": {"action": {"type": "string"}, "content": {"type": "string"}}}}] User request: store the note LOOPER-TEST-42 to memory using the memory tool.
